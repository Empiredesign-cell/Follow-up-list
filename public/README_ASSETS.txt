Copy these existing production assets into /public before final go-live if you want the exact current alarm sounds:
- absen-masuk.m4a
- alarm-pulang.mp3

The app also contains a WebAudio fallback, so it remains functional without them.
